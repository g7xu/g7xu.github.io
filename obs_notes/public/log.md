# Wiki Log

**Summary**: Append-only record of all operations on the wiki. Newest entries at the bottom.

---

## 2026-05-29

- Created `public/index.md` (table of contents) and `public/log.md` (this file).
- Indexed 29 existing pages in `public/` across Computer Vision, Machine Learning, Information Retrieval, Algorithms, Web & Backend Development, and Interview Cheat Sheets.
- Ran an initial lint of the knowledge base (see findings reported to the user).
- Acted on three lint fixes:
  - Renamed `United Testing.md` → `Unit Testing.md` (typo) and updated the index entry.
  - Fixed broken `[[MVC]]` link in `Error Handling & Validation.md` → now points to `[[JSP (Jakarta Server Pages)#MVC|MVC]]`.
  - Added reciprocal links between `Ideal Points` and `Geometric Projection Model`.
- Ran a content lint (correctness/contradictions/gaps) and applied factual fixes:
  - `Two-View Reconstruction`: relabeled "calibrated" → "uncalibrated" cameras (fundamental-matrix case); fixed `instinct` → `intrinsic`; corrected depth-from-disparity numerator `d` → `b` (baseline).
  - `Dense retrieval`: "maximize 𝓛" → "minimize the loss 𝓛" (the InfoNCE loss has a leading minus sign).
  - Renamed `Auth 2.0.md` → `OAuth 2.0.md`; corrected the intro (authorization protocol, not authentication; authentication is OIDC) and updated the index entry.
  - `ML Architecture Families`: `Principal Component Analysis (PCA)` → `Kernel PCA` (PCA is not a kernel method); `XGBoots` → `XGBoost`.
  - `Spring Framework`: Session scope "each HTTP request" → "each HTTP session"; `@PreDestory` → `@PreDestroy`.
  - `Distributed System`: Metrics monitoring `Kubesphere` → `Prometheus + Grafana`.
  - `RAG`: `replying` → `relying`. `JSP`: `jspDestory` → `jspDestroy`.
  - `Geometric Projection Model`: clarified "collapses into one dimension" → "collapses from 3D to 2D, losing one dimension".
  - `From Image Plane to Pixel Coordinates`: aligned the K-matrix legend with the actual matrix symbols ($d_x'$, $d_y'$, $c_x'$, $c_y'$).
- Restructured `Computer Vision Overview` (was a sprawling draft + lecture concatenation):
  - Rewrote it as a concise hub (intro, image-formation pipeline, analysis tasks, core challenge, links).
  - Extracted the polished "CSE 152A" lecture half into a new public page `Image Formation & Photometry` (single canonical "5 factors" list; removed the triplicated copies); added it to the index.
  - Extracted the rough draft fragments into two new `inbox/` pages pending review: `RANSAC` (robust model fitting + fundamental-matrix estimation) and `Recognition & Vision Tasks` (task taxonomy + variation challenges).
  - Removed the `TODO: delete/reconstruct` block and the unstructured fragments from the overview.
  - Wired links: overview → `Image Formation & Photometry`, `RANSAC`, `Recognition & Vision Tasks`; `Two-View Reconstruction` "Solve for F" → `RANSAC`.
- Ingested two sources added to `raw/`: `lec10_recognition.pdf` (CSE 152A Lecture 10 slides) and `en-Lecture 12 - May 7th 2026 - CSE 152A - LE [A00] (1).txt` (lecture transcript).
  - Substantially enhanced `inbox/RANSAC.md` with sourced content: noise vs. outliers, the line-fitting intuition, the general algorithm, the fundamental-matrix application, the adaptive `N = log(1−p)/log(1−(1−ε)^s)` formula + the p=0.99 iteration table, and the 8-point-vs-7-point / degrees-of-freedom efficiency rationale. Added inline (source: …) citations.
  - Substantially enhanced `inbox/Recognition & Vision Tasks.md`: the four recognition questions (all = classification), the task taxonomy with concrete examples, recognition in autonomous driving, the three kinds of variation (imaging/inter-class/intra-class), why ML replaces rules, and the 2012 data-driven inflection (datasets + deep learning + GPUs; ImageNet error → human level). Added inline citations.
  - Enhanced public `Two-View Reconstruction` "Solve for F": added the SVD least-norm solve, the imposed rank(F)=2 constraint (zero the smallest singular value), and the linear-vs-nonlinear (8 vs 7 point) rationale.
  - Both enhanced concept pages remain in `inbox/` pending review before promotion to `public/`.
- Reviewed and promoted both pages from `inbox/` to `public/Computer Vision/`: `RANSAC.md` and `Recognition & Vision Tasks.md`. Removed the inline `(source: …)` citations from both (kept the one-line Sources metadata field). Added both to the index under Computer Vision.
- Ingested Lecture 11 (`lec11_imageretrieval.pdf` + `lec11_imageretrieval_transcript.md`). Created two draft pages in `inbox/` (pending review):
  - `Discriminative vs Generative Methods.md` — statistical/Bayes viewpoint (posterior/likelihood/prior), discriminative (posterior, decision boundaries) vs generative (likelihood+prior, joint distribution), the trade-off, the three pillars (representation/learning/inference) including the "what are you maximizing?" training-objective clarification, and levels of supervision.
  - `Image Retrieval (Bag of Words).md` — instance recognition, applications, why it's hard (imaging variations + efficiency), the naive feature-matching baseline, the NLP bag-of-words origin, the 4-step bag-of-features pipeline, why a visual vocabulary (intractable matching → clustering), and the tools to come.
  - Added forward links from `Recognition & Vision Tasks` (public) to both new pages. They are not yet in the index (will add when promoted to `public/`).
- Reviewed and promoted both Lecture 11 pages from `inbox/` to `public/Computer Vision/`: `Discriminative vs Generative Methods.md` and `Image Retrieval (Bag of Words).md`. Added both to the index under Computer Vision. (Forward links from `Recognition & Vision Tasks` were already in place.) Adopted the convention of using Obsidian section links `[[#Heading]]` instead of "see below"-style pointers.
- Ingested Lecture 12 (`lec12_classification.pdf` + `lec12_classification_transcript.md`). Created three draft pages in `inbox/` (pending review):
  - `k-means Clustering.md` — unsupervised clustering: the minimize-SSE objective $D(X,C)=\sum_{C_k}\sum_{i\in C_k}(x_i-\bar c_k)^2$, the assign↔recompute iteration, no-guarantee convergence, choosing K, and its role building the visual vocabulary.
  - `Nearest-Neighbor Classification.md` — NN and kNN voting, k choice (and k≠k-means), advantages (simple/flexible/sanity-check) and limitations (time + memory; approximate methods).
  - `Bayesian Classification (MAP & ML).md` — generative classifier (skin example): want posterior, measure likelihood (histogram), Bayes' rule, prior, MAP vs ML (uniform prior). Explicitly links back to `Discriminative vs Generative Methods` and answers "how does Bayesian classification relate to the previous lecture."
  - Updated public `Image Retrieval (Bag of Words)`: replaced the "tools coming next" stub with concrete content — k-means link, histogram representation, cosine-similarity / Euclidean-distance comparison, and a precision/recall/average-precision evaluation section; wired Related-pages links to the three new pages.
  - New pages remain in `inbox/` pending review before promotion to `public/`.
- Reviewed and promoted all three Lecture 12 pages from `inbox/` to `public/Computer Vision/`: `k-means Clustering.md`, `Nearest-Neighbor Classification.md`, and `Bayesian Classification (MAP & ML).md`. Added all three to the index under Computer Vision.
- Ingested Lecture 13 (`lec13_neuralnetworks.pdf` + transcript). Created and (after review) promoted two pages to `public/Computer Vision/`:
  - `Neural Networks (Introduction).md` — learned-vs-handcrafted features, perceptron → MLP, why the nonlinearity matters, activations, bias, parameter counting, gradient descent (with a pointer to the next lecture's optimization), universal approximation, depth vs. width.
  - `Scaling Image Retrieval.md` — inverted file index, TF-IDF, vocabulary tree; plus a "why not a plain hash table?" note (nearest-neighbor vs exact-match; LSH) and a full end-to-end worked example (k-means words → tree → bag-of-words histogram → inverted index → scoring), including the three-different-"k"s caution.
  - Added both to the index. Wired links: `Recognition & Vision Tasks` ("what's next" + Related) → `Neural Networks (Introduction)`; `ML Architecture Families` (Neural Networks section) → `Neural Networks (Introduction)`; `Image Retrieval (Bag of Words)` → `Scaling Image Retrieval` (new "Making it fast at scale" section + Related).
- Ran a lint across all public pages and **reorganized `public/` into a folder taxonomy** (links unaffected — Obsidian resolves `[[…]]` by filename):
  - `Computer Vision/` now has the `Computer Vision Overview` hub at top plus three subfolders: `Image Formation & Geometry/` (7), `Features & Reconstruction/` (4), `Recognition & Retrieval/` (4).
  - The general ML tools moved from Computer Vision to `Machine Learning/`: `k-means Clustering`, `Nearest-Neighbor Classification`, `Bayesian Classification (MAP & ML)`, `Neural Networks (Introduction)`.
  - New folders for the previously-loose root pages: `Information Retrieval/` (Dense retrieval, RAG) and `Web & Backend Development/` (12 pages). Renamed `Algorithm/` → `Algorithms/` and `interview Cheat Sheet/` → `Interview Cheat Sheets/`.
  - Restructured `index.md` to mirror the folders (CV split into the three subsections; ML tools listed under Machine Learning).
  - Lint notes: no orphans; no new broken links (the remaining unresolved links are the `inbox/`-pending `Convolutional Neural Network (CNN)`, `kernel function (TODO)`, `Algorithm CheatSheet (TODO)`). Older geometry/web pages still lack the `**Summary**`/`**Last updated**` header block — a separate backfill if desired.
- Addressed the carry-over lint items:
  - **Format backfill:** added a `**Summary**` + `**Last updated**` header block to the 28 pages that lacked one (geometry CV, features, ML, Information Retrieval, Web & Backend, Algorithms, Interview Cheat Sheets). `Last updated` dates taken from each file's modification time. Every public page now has a header.
  - **Cross-linking:** added `## Related pages` sections knitting the Web & Backend pages together (Spring ↔ JSP ↔ Error Handling ↔ MAVEN ↔ OAuth ↔ Unit Testing; Deployment ↔ Distributed System ↔ SDLC ↔ Git), plus Dense retrieval ↔ RAG and the two interview sheets.
  - **Mis-pointed link fixed:** in `ML Architecture Families`, the Kernel Methods section linked `[[kernel function (TODO)]]`, but that inbox note is about the *convolution* kernel — a different concept from the SVM kernel trick. De-linked to plain text ("a kernel function / the kernel trick").
  - Left `Convolutional Neural Network (CNN)` and `Algorithm CheatSheet (TODO)` as legitimate forward-references (both are the user's in-progress `inbox/` drafts and not yet covered by a lecture; not fabricated).
- Ingested Lecture 14 (`lec14_optimizationcnn.pdf` + transcript) — content is entirely **Optimization in Neural Networks** (no CNN despite the filename; that's a later lecture). Rather than a separate page, **merged it with the NN intro into a single `Machine Learning/Neural Network.md`** (per user request to "just call it Neural Network"):
  - The page now runs architecture → expressivity → training: motivation, perceptron → MLP, nonlinearity, activations, bias, parameter counting, universal approximation (with the box→ReLU construction), then **Training a neural network** — cost (MSE), gradient descent + update rule + learning rate, SGD/mini-batches, pitfalls (local minima/saddle points, ill-conditioning/condition number, SGD noise), momentum + LR scheduling, and backpropagation (chain rule; "backprop computes gradients, SGD trains"; worked example deferred to a later lecture).
  - Deleted `Neural Networks (Introduction).md` and the `inbox/Neural Network Optimization.md` draft.
  - Updated all links to the new name: `index.md`, `ML Architecture Families`, `Recognition & Vision Tasks` (×2). Verified no dangling links remain.
- Ingested Lecture 15 (`lec15_cnn.pdf` + transcript) — backpropagation worked example + Convolutional Neural Networks intro:
  - Enriched the `Neural Network` backpropagation section with a worked computational-graph example ($f=(x+y)z$ with numbers, forward + backward pass) and the gate patterns (add = distributor, multiply = switcher, max = router).
  - Created `Machine Learning/Convolutional Neural Network (CNN).md` (replacing the user's inbox stub): why learn filters (Sobel → learned), convolution recap, why convolution fits images (local connectivity / spatial / channels), FC vs conv layer, image → feature-volume mechanics (k×k → W×H×C, 3D filters on volumes), parameter counting (147 → 735 example), weight sharing + the DeepFace exception; "what's next" stub preserves the user's receptive-field to-do. Added to the index.
  - This **resolves** the previously-dangling `[[Convolutional Neural Network (CNN)]]` forward-reference from `ML Architecture Families`. Wired `Neural Network` ↔ `CNN`. Only remaining unresolved link is the user's `Algorithm CheatSheet (TODO)` inbox draft.
- Ingested Lecture 16 (`lec16_regularization.pdf` + transcript) — content is actually CNN continuation + transfer learning + the train/val/test setup (regularization itself is deferred to the next lecture, filename notwithstanding):
  - Enriched `Convolutional Neural Network (CNN)`: added convolution-is-differentiable, pooling (2×2/stride-2, no params, per-map, max-gate gradient, three reasons), receptive field (deeper = broader, "context"), 1×1 convolution (channel change / multimodal fusion), and the classification head (flatten → fully-connected → softmax with the formula). Retired the receptive-field to-do.
  - Created `Machine Learning/Transfer Learning.md` — pretrained features, ImageNet source model, the retrain-by-data-budget table (final layer → FC → deeper conv → full fine-tune), learning-rate as inertia-vs-responsiveness.
  - Created `Machine Learning/Generalization & Model Validation.md` — train/validation/test split, why you never tune on test (radiologist analogy), generalization vs. accuracy; stub ready for next lecture's regularization.
  - Added both to the index. Wired: `CNN` → both; `Neural Network` → both; `ML Overview` → `Generalization`; `Transfer Learning` ↔ `Generalization`. No new dangling links (only the user's `Algorithm CheatSheet (TODO)` remains).
- Enriched `inbox/Atlas (Boston Dynamics).md` by ingesting two more `raw/` sources — *"Atlas' Evolution From Research Robot to Industrial Humanoid"* (BD blog, 2026-03-17) and *"Boston Dynamics Unveils New Atlas Robot to Revolutionize Industry"* (BD press release, 2026-01-05):
  - The page graduated from "single-source seed" to a developed draft. Added an **evolution timeline** (hydraulic research → April 2024 all-electric reset → 2025 part-sequencing pilot at Hyundai's Georgia metaplant → Jan 2026 production), full **production specs** (now incl. 2.3 m reach), an **operation** section (3 control modes, autonomous battery hot-swap, Orbit/MES/WMS, fleet learning, fenceless safety), a **training/brain** section (RL in sim + teleop demos + large behavior models + foundation models), and **partners & scale** (Hyundai, Hyundai Mobis actuators, 30k-robots/yr factory, $26B).
  - **Resolved the "new AI partner" unknown:** it is **Google DeepMind** — the lab behind [[RT-2]] — which makes a concrete bridge from the Atlas hardware page to the vault's VLA cluster. Strengthened the `[[RT-2]]` cross-link accordingly.
  - Still in `inbox/` pending review. Two Atlas raw sources remain unincorporated (3D-vision upgrade; "Atlas, meet Atlas" is now fully folded in).
- **Integrated the robotics pages into the public graph as a new domain.** Created `public/Robotics & Embodied AI/` (a peer of Computer Vision / Machine Learning) and promoted `RT-2.md` and `Atlas (Boston Dynamics).md` out of `inbox/` into it.
  - Built `Robotics & Embodied AI Overview.md` as the domain **sub-index** (tagged `#moc`), reusing the Computer Vision pilot pattern: grouped into **Methods & Models** (the "brains" — [[RT-2]], queued OpenVLA/CogACT/OE-VLA) and **Platforms & Hardware** (the "bodies" — [[Atlas (Boston Dynamics)]]), with a "Builds on → [[Neural Network]] → [[CNN]] → VLMs → VLA" bridge into the Machine Learning cluster.
  - `index.md` gets exactly **one** new line (`[[Robotics & Embodied AI Overview]]`), keeping the root map flat per the layered sub-index design.
  - **VLM**: included a short Vision-Language-Model explainer inside the sub-index plus a `[[Vision-Language Model (VLM)]]` forward-reference — but deliberately did **not** create the page, since it would lack a `raw/` source to cite (violates the citation rule). To be grounded later from OpenVLA or a VLM survey PDF.
  - Tightened the Atlas **Safety** bullet: fenceless guarding = sensing-replaces-cages + speed/force backstop, with an explicit **needs-verification** note on the unstated speed/force limits and ISO 10218 / ISO/TS 15066 certification.
- Folded the 4th Atlas source — *"Atlas robot gets superhuman 3D vision in major Boston Dynamics upgrade"* (Interesting Engineering, 2025-05-28) — into `Robotics & Embodied AI/Atlas (Boston Dynamics).md`:
  - Added a **Perception: how Atlas sees** section covering the four-stage vision pipeline: 2D detection (bounding boxes + outer/inner keypoints), 3D localization (keypoint-to-model alignment, occlusion handling, spatial memory), SuperTracker object tracking (kinematic+visual+force fusion, CAD-render matching, millimeter accuracy), and hand-eye calibration (compensating wear/temperature/variance).
  - **New cross-domain bridge: Robotics ↔ Computer Vision.** Wired the perception section into the CV cluster — [[Recognition & Vision Tasks]], [[Feature Detection]], [[Feature Matching]], [[Two-View Reconstruction]], [[From Image Plane to Pixel Coordinates]], [[Extrinsics]], [[Computer Vision Overview]] — so the applied robot-vision page links back to the theory pages (previously these two domains were disconnected).
  - Highlighted the source's punchline — a "unified foundation model where seeing and doing aren't separate" — as the in-text bridge to [[RT-2]] / the VLA thesis and the DeepMind brain section. Added the source to the page's Sources line; noted its May-2025 date (pilot phase, pre-production).
- Ingested the VLM survey — Ghosh et al., *"Exploring the Frontier of Vision-Language Models"* (arXiv:2404.07214) — and drafted `inbox/Vision-Language Model (VLM).md` (pending review). This is the source that grounds the previously-dangling `[[Vision-Language Model (VLM)]]` forward-reference from the robotics sub-index.
  - Covers: why VLMs exist (LLMs are text-only) and the not-all-multimodal-is-a-VLM boundary (DALL-E/Midjourney excluded); the general architecture (image+text encoders → fusion layer → LLM) with the frozen/trainable axis and the three canonical fusion methods (Q-Former→BLIP-2, Perceiver Resampler→Flamingo, MLP→LLaVA); the survey's 3-category taxonomy (Understanding / Text-gen-from-multimodal / Multimodal-out); the canonical model lineage (CLIP, Flamingo, BLIP-2, PaLI, PaLM-E, LLaVA, GPT-4V, Gemini); and future directions incl. a Safety subsection (adversarial-image jailbreaks, visual prompt-injection, multimodal hallucination).
  - Wired as the **conceptual root of the robotics cluster**: PaLM-E (embodied VLM, robotic manipulation planning) is the direct ancestor of [[RT-2]]; VLA = a generative VLM with an action-token vocabulary. Cross-linked to RT-2, the robotics sub-index, Discriminative vs Generative, Transfer Learning, Neural Network, CNN, and Atlas (safety theme).
  - Not yet promoted to `public/` or added to `index.md` — awaiting review.
- Promoted `Vision-Language Model (VLM).md` from `inbox/` into `public/Robotics & Embodied AI/`. Resolved the previously-dangling `[[Vision-Language Model (VLM)]]` forward-reference: it now appears as a real **Methods & Models** entry in the robotics sub-index (above RT-2) and is linked from the "Builds on" bridge. No remaining dangling links in the robotics cluster.
- Ingested OpenVLA — Kim et al., *"OpenVLA: An Open-Source Vision-Language-Action Model"* (arXiv:2406.09246) — and drafted `inbox/OpenVLA.md` (pending review). #2 in the VLA paper queue; the open-source response to RT-2.
  - Covers: architecture (Prismatic-7B VLM = Llama 2 7B + fused DINOv2/SigLIP encoder + MLP projector → 7-DoF action); action tokenization (256 bins via 1st–99th quantile, overwrite Llama's 256 least-used tokens — RT-2's trick refined); headline result (beats closed RT-2-X 55B by +16.5% with 7× fewer params); the real contribution (LoRA fine-tuning at 1.4% of params on a single A100, 4-bit quantization to 7GB VRAM, fully open data+weights+code); surprising ablations (fine-tune the vision encoder don't freeze it; ~27 epochs; resolution doesn't help); limitations.
  - Captured the **key tension with RT-2**: RT-2-X still wins on *semantic* generalization because it co-fine-tunes on web data, while OpenVLA trains on robot data alone — direct evidence for RT-2's co-fine-tuning insight.
  - Cross-linked to [[RT-2]], [[Vision-Language Model (VLM)]], [[Transfer Learning]] (LoRA), [[Robotics & Embodied AI Overview]]; forward refs to [[CogACT]], [[OE-VLA]]. Not yet promoted to public.
- Promoted `OpenVLA.md` from `inbox/` into `public/Robotics & Embodied AI/`. Upgraded the sub-index's "(queued) OpenVLA" into a real linked Methods & Models entry (between RT-2 and the remaining queue). Its `[[RT-2]]` / `[[Vision-Language Model (VLM)]]` / `[[Transfer Learning]]` links all resolve. Remaining queue: CogACT, OE-VLA.
- Ingested CogACT — Li et al., *"CogACT: A Foundational Vision-Language-Action Model for Synergizing Cognition and Action…"* (arXiv:2411.19650) — and drafted `inbox/CogACT.md` (pending review). #3 in the VLA queue; the "react to" paper.
  - Core thesis captured: it **critiques the action-as-text-tokens design of [[RT-2]]/[[OpenVLA]]** (actions are continuous/multimodal/temporally-correlated, so quantization limits precision) and instead **decouples cognition from action** — VLM emits a cognition token that conditions a specialized **Diffusion Transformer (DiT)** action module that denoises a sequence of future actions.
  - Covered: the 3-module componentized architecture (DINOv2+SigLIP vision → Llama-2 + cognition token → DiT action head; end-to-end MSE/diffusion loss); results (beats same-size OpenVLA by >35% sim / 55% real, beats RT-2-X 55B by 18% sim); the favorable action-module scaling insight (scale the action head, not the whole VLM; ~linear in log size); and the Adaptive Action Ensemble (cosine-similarity-weighted fusion of current+past predictions).
  - Framed the **VLA design fork**: token branch (RT-2, OpenVLA) vs cognition–action/diffusion branch (CogACT), noting it shares OpenVLA's backbone so it's a clean A/B on the action representation.
  - Cross-linked to OpenVLA, RT-2, VLM, robotics sub-index; forward refs [[OE-VLA]] and a new [[Diffusion Models]] (concept that may warrant its own page). Not yet promoted.
- Promoted `CogACT.md` from `inbox/` into `public/Robotics & Embodied AI/`. Upgraded the sub-index's "(queued) CogACT" into a real linked Methods & Models entry. Its `[[OpenVLA]]`/`[[RT-2]]`/`[[VLM]]` links resolve; the one intentional dangling link in the cluster is the new `[[Diffusion Models]]` forward-reference. Remaining queue: OE-VLA.
- Ingested OE-VLA — Zhao et al., *"Unveiling the Potential of Vision-Language-Action Models with Open-Ended Multimodal Instructions"* (arXiv:2505.11214) — drafted and immediately promoted `OE-VLA.md` into `public/Robotics & Embodied AI/`. #4 / final paper in the VLA queue.
  - Core: prior VLAs accept only language; OE-VLA follows **open-ended multimodal instructions** (object-as-image, whiteboard/optical text, goal image, video demo) — widening the *input* axis. Four task types (VOS/OIF/VGR/VDL); LLaVA-Next-Interleave + SigLIP + Qwen-1.5 (32k ctx); token-based actions (deliberately NOT a diffusion head — opposite of CogACT, to isolate the instruction-modality variable); two-stage curriculum (multi-image grounding → open-ended instruction tuning); dataset-conversion recipe; new OE-CALVIN base/hard benchmarks. Result: best on standard language CALVIN (2.99 vs OpenVLA 0.90), strong on VOS/OIF/VDL, VGR hardest.
  - Added the **VLA design-axis summary** to the sub-index: RT-2 = paradigm; OpenVLA = open & efficient; CogACT = action representation (diffusion); OE-VLA = instruction modality (multimodal). The Methods & Models group is now complete (RT-2 → OpenVLA → CogACT → OE-VLA).
  - **VLA paper queue is finished.** Remaining robotics raw sources are the landscape clippings (State of Robotics 2026, CVPR 2026 ×2, National Robotics Week). Only intentional dangling link in the cluster: [[Diffusion Models]].

## 2026-06-09

- Ingested **Lecture 17 — "Regularization"** (`lec17_regularization.pdf`, 56 slides + `lec17_regularization.md` transcript). The recap material (softmax-as-max, pooling, receptive field, gradient descent / SGD+momentum, the over-fitting MNIST demo) is already covered in `Convolutional Neural Network (CNN)` / `Neural Network` / `Generalization & Model Validation`, so the genuinely new content was split into two new pages in `public/Machine Learning/` (following the Lecture-16 precedent of writing lecture pages straight to public rather than inbox):
  - Created **`Machine Learning/Regularization.md`** — overfitting framed as over-parameterized "lazy learners" taking shortcuts; reduce complexity *without* cutting capacity (the degree-9 vs degree-2 polynomial analogy); **L2** (cost/gradient/update, weight-decay ∝ w, "spheres out" the weights); **L1** (sgn gradient, constant shrink → sparsity); L1-vs-L2 and $L_p$ ($L_{0.5}$ even sparser); **dropout** (delete ~half the neurons per iteration, restore + halve weights at test; ensemble + breaks neuron co-adaptation); **data augmentation** (flips / random crops+scales / color jitter / rotation-shear-blur, and the "not free data — the net can learn the augmentation rule" caveat); closes with the three-lever table (weights / architecture / data).
  - Created **`Machine Learning/CNN Architectures.md`** — LeNet (OCR / mail sorting) → AlexNet (2012 ImageNet, 18.2% top-5, 60M params, GPU) with the **layer-removal ablation** (drop FC7 −1.1%/−16M; FC6+7 −5.7%/−50M; conv3+4 −3%/−1M; all four −33.5% → "Depth!"; most params in FC, but conv layers are the engine) → **width-vs-depth** (universal approximation; depth needs polynomial vs exponential neurons, Telgarsky 2016) → **VGG** (3×3-only, 19 layers, 6.8% top-5; 9 vs 49 params) → the **depth problem** (plain deep nets have *higher training* error = optimization, not overfitting; three receptive-field levers cost detail / params / vanishing gradients; human-pose-estimation motivation) → **ResNet** (residual $H(x)=F(x)+x$, identity skip, easier optimization, 152 layers) → "bigger ≠ better" + a train-well debugging checklist (Ranzato). Noted that the transcript stops at VGG ("next lecture") while the deck carries ResNet.
  - Wired the cluster: **resolved the standing placeholder** in `Generalization & Model Validation` ("regularization … will be added here") into a real `[[Regularization]]` link + lazy-learner framing; updated `Convolutional Neural Network (CNN)`'s "What's next" to point at `[[CNN Architectures]]` and `[[Regularization]]`. The two new pages cross-link each other and back to CNN / Neural Network / Transfer Learning / Generalization.
  - Added both pages to `index.md` under Machine Learning and bumped its date. No new dangling links (only the pre-existing `[[Diffusion Models]]` forward-ref in the robotics cluster and the user's `Algorithm CheatSheet (TODO)` inbox stub remain).
